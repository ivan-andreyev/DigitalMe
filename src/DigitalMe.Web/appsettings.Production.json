{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning",
      "Microsoft.AspNetCore.SignalR": "Information"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Host=${DB_HOST};Port=${DB_PORT};Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASSWORD};Pooling=true;Minimum Pool Size=10;Maximum Pool Size=200;Connection Idle Lifetime=30;Connection Pruning Interval=10;Include Error Detail=false;Timeout=30;CommandTimeout=30;",
    "ReadReplica": "Host=${DB_READ_HOST:-${DB_HOST}};Port=${DB_READ_PORT:-${DB_PORT}};Database=${DB_NAME};Username=${DB_READ_USER:-${DB_USER}};Password=${DB_READ_PASSWORD:-${DB_PASSWORD}};Pooling=true;Minimum Pool Size=5;Maximum Pool Size=100;Connection Idle Lifetime=30;Connection Pruning Interval=10;Include Error Detail=false;Timeout=30;CommandTimeout=30;",
    "Redis": "redis-server:6379"
  },
  "AllowedHosts": "*",
  "DigitalMe": {
    "ApiBaseUrl": "https://digitalme-api-223874653849.us-central1.run.app",
    "SignalRHub": "/chathub",
    "Authentication": {
      "JwtSecret": "${JWT_SECRET}",
      "Issuer": "DigitalMe",
      "Audience": "DigitalMe-Web"
    },
    "Features": {
      "UseRealSignalR": true,
      "UseRealAuthentication": true,
      "EnableLogging": false
    },
    "Performance": {
      "EnableResponseCaching": true,
      "CacheExpirationMinutes": 30,
      "EnableOutputCaching": true,
      "EnableResponseCompression": true
    },
    "Database": {
      "CommandTimeout": 30,
      "ConnectionPoolSize": 200,
      "EnableRetryOnFailure": true,
      "MaxRetryCount": 3,
      "MaxRetryDelay": "00:00:05"
    }
  }
}