---
description: "Применять при работе с терминалом/командной строкой. Ключевые слова для активации: 'git', 'терминал', 'команда зависла', 'команда не выполняется', 'остаточные символы', 'сgit', 'timeout', 'коммит', 'git add', 'git status', 'PowerShell', 'cmd', 'выполнить команду', 'terminal', 'command line', 'shell'. Автоматически применяется при обнаружении проблем с выполнением команд: зависание >15 секунд, ошибки типа 'command not found' с лишними символами, прерывание команд через Ctrl+C."
alwaysApply: false
---

# Terminal Management Rules

## Основные принципы работы с терминалом

### 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ И ИХ РЕШЕНИЯ

#### Проблема: Остаточные символы после Ctrl+C
**Симптом**: Команды начинаются с лишних символов (например `сgit` вместо `git`)
**Причина**: После прерывания команды символы остаются в буфере терминала
**Решение**: 
```
1. Выполнить "холостую" команду для очистки буфера
2. НЕ ЖДАТЬ её завершения
3. Выполнить целевую команду
```

#### Проблема: Зависание команд
**Симптом**: Команда не завершается, но система не показывает ошибку
**Причина**: Команда ожидает ввода или заблокирована
**Решение**: Использовать timeout pattern

### 📋 ОБЯЗАТЕЛЬНЫЕ ПРАКТИКИ

#### 1. Предварительная очистка терминала
Перед важными командами всегда выполнять:
```powershell
# Очистка буфера
echo "clear" 
# Затем основная команда
git commit -m "message"
```

#### 2. Timeout для быстрых команд
Команды, которые должны завершаться за <10 секунд:
- `git status`
- `git add`
- `git commit`
- `ls` / `dir`
- `cd`
- `echo`

**Обязательно прерывать, если нет ответа >15 секунд**

#### 3. Проверка доступности команд
Перед использованием команды проверить:
```powershell
Get-Command git -ErrorAction SilentlyContinue
```

#### 4. Fallback стратегии
Если команда зависает:
1. Прервать выполнение
2. Использовать альтернативную команду
3. Объяснить пользователю проблему
4. Предложить ручное выполнение

### 🛠️ TERMINAL WRAPPER PATTERN

#### Безопасный wrapper для Git команд
```powershell
# Очистка + проверка + выполнение
function Safe-Git {
    param($Command)
    
    # 1. Очистка буфера
    Write-Output "Preparing..."
    
    # 2. Проверка доступности git
    if (-not (Get-Command git -ErrorAction SilentlyContinue)) {
        throw "Git not available"
    }
    
    # 3. Выполнение с timeout
    $job = Start-Job -ScriptBlock { git $using:Command }
    if (Wait-Job $job -Timeout 30) {
        Receive-Job $job
    } else {
        Stop-Job $job
        throw "Command timed out"
    }
}
```

### 📝 COMMAND EXECUTION CHECKLIST

#### Перед выполнением ЛЮБОЙ команды:
- [ ] Команда простая и не требует интерактивного ввода?
- [ ] Ожидаемое время выполнения <10 секунд?
- [ ] Есть ли риск зависания?
- [ ] Нужна ли предварительная очистка терминала?

#### При зависании команды:
- [ ] Прервать выполнение немедленно
- [ ] Объяснить пользователю проблему
- [ ] Предложить альтернативное решение
- [ ] Задокументировать проблему

#### После проблемной команды:
- [ ] Проверить состояние терминала
- [ ] Очистить буфер перед следующей командой
- [ ] Убедиться в корректности последующих команд

### 🚀 BEST PRACTICES

#### 1. Простые команды в приоритете
```powershell
# ХОРОШО: простые, быстрые команды
git status
git add .
git commit -m "message"

# ПЛОХО: сложные, потенциально зависающие
find . -name "*.cs" -exec grep -l "pattern" {} \;
```

#### 2. Разбивка сложных операций
```powershell
# Вместо одной сложной команды
Get-ChildItem -Recurse | Where-Object {$_.Length -gt 1MB}

# Использовать последовательность простых
Get-ChildItem -Recurse
# Затем фильтрация в отдельной команде
```

#### 3. Проактивное управление сессией
```powershell
# Периодически обновлять состояние терминала
pwd  # Проверить текущую директорию
echo $LASTEXITCODE  # Проверить код выхода
```

### 🔧 EMERGENCY PROTOCOLS

#### Если терминал "сломался":
1. **НЕ ПАНИКОВАТЬ** - объяснить пользователю ситуацию
2. **ДИАГНОСТИРОВАТЬ** - что именно не работает
3. **FALLBACK** - предложить альтернативные способы
4. **ДОКУМЕНТИРОВАТЬ** - записать проблему для будущего

#### Альтернативные способы выполнения:
- Использование других инструментов (PowerShell vs CMD)
- Ручное выполнение пользователем
- Разбивка на мелкие операции
- Использование GUI инструментов

### 📊 МОНИТОРИНГ И ОТЛАДКА

#### Логирование проблемных команд:
Ведите учет команд, которые вызывают проблемы:
- Команда
- Время выполнения
- Тип проблемы
- Решение

#### Паттерны проблем:
- Команды с wildcards (`*.ext`)
- Pipe операции (`|`)
- Длинные пути
- Специальные символы в аргументах

---

## ИТОГОВОЕ ПРАВИЛО

**ВСЕГДА**: Предполагать, что команда может зависнуть
**НИКОГДА**: Ждать команду дольше разумного времени
**ОБЯЗАТЕЛЬНО**: Предупреждать пользователя о проблемах
**ПОМНИТЬ**: Терминал - ненадежный инструмент, нужны fallback стратегии