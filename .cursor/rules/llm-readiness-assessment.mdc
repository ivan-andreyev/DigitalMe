---
description: "Унифицированный алгоритм оценки LLM READINESS для планов работ. АВТОМАТИЧЕСКИ применяется при: анализе планов, оценке готовности к реализации, валидации документации. Ключевые слова для активации: 'LLM readiness', 'готовность плана', 'оценка плана', 'llm готовность', 'план готов', 'readiness assessment', 'план к реализации', 'оценить готовность'."
alwaysApply: false
---
# LLM Readiness Assessment Algorithm

Унифицированный алгоритм оценки готовности планов к использованию LLM для автоматической реализации.

## 🎯 ОСНОВНАЯ ФОРМУЛА

```
LLM_Readiness = (
    (100 - FileSize_Violation_Percentage) * 0.25 +     // Структурная готовность
    (FileLine_Reference_Coverage) * 0.25 +             // Конкретность спецификаций
    (100 - Generic_Term_Percentage) * 0.25 +           // Техническая детализация
    (Success_Criteria_Coverage) * 0.25                 // Верифицируемость
) = Score%
```

## 🚨 ОСОБЕННОСТЬ: НОВЫЙ ФУНКЦИОНАЛ

**⚡ КРИТИЧНО**: Для планов описывающих НОВЫЙ функционал применяется **модифицированная формула**:

```
LLM_Readiness_New = (
    (100 - FileSize_Violation_Percentage) * 0.30 +     // +5% вес (30%)
    (Architectural_Specification_Quality) * 0.25 +     // Замена File:Line на архитектуру
    (100 - Generic_Term_Percentage) * 0.30 +           // +5% вес (30%) 
    (Success_Criteria_Coverage) * 0.15                 // -10% вес (15%)
) = Score%
```

### 🔍 Определение "НОВЫЙ ФУНКЦИОНАЛ":
- ✅ Файлы содержат TODO-маркеры и архитектурные заглушки
- ✅ Описывают компоненты которых ещё нет в кодовой базе
- ✅ Содержат `throw new NotImplementedException()`
- ✅ Планируют создание новых файлов/классов/интерфейсов

## 📊 КРИТЕРИИ КОМПОНЕНТОВ

### 1️⃣ **File Size Compliance** (25%/30% для нового)
- **Источник**: `@common-plan-reviewer.mdc:64`
- **Критерий**: Все файлы плана ≤400 строк
- **Расчет**: `(файлов_соответствующих / общее_количество) * 100`
- **Целевое значение**: 100%

```powershell
# Проверка размера файлов
Get-ChildItem "Docs/PLAN" -Recurse -Filter "*.md" | ForEach-Object {
    $lineCount = (Get-Content $_.FullName | Measure-Object -Line).Lines
    if ($lineCount -gt 400) { Write-Host "❌ $($_.Name): $lineCount строк" }
}
```

### 2️⃣ **File:Line Reference Coverage** (25% для существующего)
- **Критерий**: ≥4 конкретные ссылки на файл
- **Формат**: `Game.Framework/Domain/Component.cs:lines X-Y`
- **Целевое значение**: 90%+ файлов

```powershell
# Подсчет file:line ссылок
$references = Select-String -Path "Docs/PLAN/**/*.md" -Pattern "\.cs:\d+(-\d+)?" -AllMatches
$filesWithRefs = $references | Group-Object Path | Where-Object Count -ge 4
```

### 2️⃣ **Architectural Specification Quality** (25% для нового)
**Замена File:Line для новых функционалов:**
- **Критерий**: Детализированные архитектурные спецификации
- **Элементы**:
  - ✅ Интерфейсы с полными сигнатурами методов
  - ✅ Классы с структурой полей и свойств  
  - ✅ Диаграммы взаимодействия компонентов
  - ✅ Паттерны проектирования с примерами
  - ✅ Конфигурация DI/IoC контейнеров

**Формула качества архитектуры:**
```
Architectural_Quality = (
    Interface_Coverage * 0.30 +        // Покрытие интерфейсами
    Class_Structure_Detail * 0.25 +    // Детализация структуры классов
    Pattern_Implementation * 0.20 +     // Реализация паттернов
    DI_Configuration * 0.15 +           // Конфигурация зависимостей
    Documentation_Depth * 0.10         // Глубина документации
) * 100
```

### 3️⃣ **Generic Term Elimination** (25%/30% для нового)
- **Критерий**: <100 общих терминов
- **Общие термины**: "implement", "create", "add", "update", "система", "компонент"
- **Целевое значение**: 98.9% редукция от исходного количества

```powershell
# Поиск общих терминов
$genericTerms = @("implement", "create", "add", "update", "система", "компонент", "functionality", "feature")
$totalGeneric = 0
foreach ($term in $genericTerms) {
    $count = (Select-String -Path "Docs/PLAN/**/*.md" -Pattern $term -AllMatches).Count
    $totalGeneric += $count
}
```

### 4️⃣ **Success Criteria Coverage** (25%/15% для нового)
- **Критерий**: ≥1 верифицируемый критерий на файл
- **Ключевые фразы**: "verified by", "tested by", "validated by", "measured by"
- **Целевое значение**: 100% файлов

```powershell
# Поиск критериев успеха
$criteriaPatterns = @("verified by", "tested by", "validated by", "measured by")
$filesWithCriteria = @()
foreach ($pattern in $criteriaPatterns) {
    $matches = Select-String -Path "Docs/PLAN/**/*.md" -Pattern $pattern
    $filesWithCriteria += $matches.Path
}
```

## 🎯 ПОРОГОВЫЕ ЗНАЧЕНИЯ

| Уровень | Score | Статус | Действия |
|---------|-------|--------|----------|
| **APPROVED** | ≥90% | ✅ Готов к реализации | Можно передавать LLM |
| **REQUIRES_REVISION** | 60-89% | ⚠️ Требует доработки | Исправить критичные проблемы |
| **CRITICAL** | <60% | ❌ Критическое состояние | Полная переработка |

## 🔧 АВТОМАТИЧЕСКАЯ ВАЛИДАЦИЯ

### Первый шаг - запуск скрипта:
```powershell
PowerShell -ExecutionPolicy Bypass -File ".cursor/tools/PlanStructureValidator.ps1"
```

### Комплексная проверка LLM READINESS:
```powershell
PowerShell -ExecutionPolicy Bypass -File "Docs/Scripts/validate-llm-execution-readiness.ps1"
```

## ✅ КАЧЕСТВЕННЫЕ КРИТЕРИИ

### **ПРАВИЛЬНО для плана** (готово к LLM):
- ✅ Сигнатуры методов с TODO комментариями
- ✅ Структуры классов с описанием назначения
- ✅ Архитектурные схемы и интерфейсы  
- ✅ Заглушки: `throw new NotImplementedException()`
- ✅ Конкретные технические спецификации
- ✅ Измеримые критерии успеха

### **НЕПРАВИЛЬНО для плана** (не готово к LLM):
- ❌ Полные реализации методов с бизнес-логикой
- ❌ Детальные алгоритмы и LINQ запросы  
- ❌ Готовый продакшн код
- ❌ Абстрактные описания без деталей
- ❌ Общие фразы типа "нужно реализовать"

## 🚨 КРИТИЧЕСКИЕ ИСКЛЮЧЕНИЯ

### ❌ НЕ СНИЖАЮТ LLM READINESS:
- **TODO-маркеры** в планах (это правильно!)
- **NotImplementedException** в архитектурных заглушках
- **Отсутствие полной реализации** (план ≠ реализация)
- **Ссылки на несуществующие файлы** для новых функционалов

### ✅ ОБЯЗАТЕЛЬНО СНИЖАЮТ LLM READINESS:
- Файлы >400 строк
- Отсутствие конкретных технических деталей
- Общие термины без спецификаций
- Отсутствие измеримых критериев

## 📈 АЛГОРИТМ ОПРЕДЕЛЕНИЯ ТИПА ПЛАНА

```powershell
function Get-PlanType {
    param([string]$PlanPath)
    
    $content = Get-Content $PlanPath -Raw
    $newFunctionalityIndicators = @(
        "TODO:",
        "NotImplementedException",
        "new interface",
        "new class", 
        "будет создан",
        "планируется реализовать"
    )
    
    $existingFunctionalityIndicators = @(
        "\.cs:\d+(-\d+)?",  # file:line references
        "существующий файл",
        "текущая реализация"
    )
    
    $newScore = ($newFunctionalityIndicators | ForEach-Object { 
        ([regex]::Matches($content, $_, [System.Text.RegularExpressions.RegexOptions]::IgnoreCase)).Count 
    } | Measure-Object -Sum).Sum
    
    $existingScore = ($existingFunctionalityIndicators | ForEach-Object { 
        ([regex]::Matches($content, $_, [System.Text.RegularExpressions.RegexOptions]::IgnoreCase)).Count 
    } | Measure-Object -Sum).Sum
    
    if ($newScore -gt $existingScore * 1.5) {
        return "NEW_FUNCTIONALITY"
    } else {
        return "EXISTING_FUNCTIONALITY" 
    }
}
```

## 🎯 ЦЕЛЕВЫЕ ПОКАЗАТЕЛИ

### Для существующего функционала (90%+ LLM READINESS):
1. **File Size**: 100% файлов ≤400 строк
2. **File:Line References**: 4+ ссылок на файл
3. **Generic Terms**: <100 общих терминов  
4. **Success Criteria**: 100% файлов с критериями

### Для нового функционала (90%+ LLM READINESS):
1. **File Size**: 100% файлов ≤400 строк
2. **Architectural Quality**: 90%+ детализированных спецификаций
3. **Generic Terms**: <50 общих терминов (повышенные требования)
4. **Success Criteria**: 85%+ файлов с критериями

## 🔄 ИНТЕГРАЦИЯ С ДРУГИМИ ПРАВИЛАМИ

- **Структура**: См. `@catalogization-rules.mdc` и `@common-plan-reviewer.mdc`
- **Генерация**: См. `@common-plan-generator.mdc`
- **Архитектура**: См. `@architecture-documenter.mdc`

**Применение в других файлах:**
```markdown
**LLM READINESS**: См. детали в `@llm-readiness-assessment.mdc`
```

## 📊 ОТЧЕТНОСТЬ

### Формат отчета:
```
=== LLM READINESS ASSESSMENT ===
Plan Type: [NEW_FUNCTIONALITY|EXISTING_FUNCTIONALITY]
Total Score: XX.X%
Status: [APPROVED|REQUIRES_REVISION|CRITICAL]

Components:
- File Size Compliance: XX.X/25.0 (XX%)
- Reference/Architecture Coverage: XX.X/25.0 (XX%)  
- Generic Term Elimination: XX.X/25.0 (XX%)
- Success Criteria Coverage: XX.X/25.0 (XX%)

Recommendations:
[Конкретные шаги для улучшения]
```