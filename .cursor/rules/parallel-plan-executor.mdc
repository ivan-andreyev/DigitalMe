---
description: "Применять для создания параллельных потоков выполнения планов на основе анализа зависимостей задач. АВТОМАТИЧЕСКИ применяется при: планах >5 задач, временных оценках >7 дней, обнаружении ключевых слов. Ключевые слова для активации: 'параллельное выполнение', 'распараллелить план', 'независимые задачи', 'потоки работы', 'concurrent execution', 'параллельно', 'одновременно', 'многопоточность', 'оптимизация времени'."
alwaysApply: false
---
# Parallel Plan Executor

Применяется для автоматического распараллеливания планов на основе анализа зависимостей задач.

## 🚀 АЛГОРИТМ РАСПАРАЛЛЕЛИВАНИЯ

### ШАГ 1: Запуск специализированного агента
```
ОБЯЗАТЕЛЬНО использовать Task tool для запуска агента:

subagent_type: "parallel-plan-optimizer" 
description: "Optimize plan for parallel execution"
prompt: "Проанализируй план [НАЗВАНИЕ] и создай оптимизированную параллельную структуру с независимыми flows. Построй матрицу зависимостей, найди критический путь, создай мета-план с синхронизационными точками."
```

### ШАГ 2: Автономная работа агента
Специализированный агент выполняет:
```
1. Извлечение всех задач из плана и дочерних файлов
2. Идентификация входных зависимостей каждой задачи
3. Построение графа зависимостей (DAG)
4. Определение критического пути выполнения
5. Создание матрицы блокировок  
6. Анализ архитектурных зависимостей (применить правила `@architecture-documenter.mdc`)
7. Группировка задач в оптимальные flows с учетом архитектурных ограничений
8. Генерация мета-плана с параллельной структурой
```

### ШАГ 3: Результат работы агента
Агент автоматически создает:

#### Матрицу блокировок:
| Задача | Входные зависимости | Блокирует | Временная оценка |
|--------|---------------------|-----------|------------------|
| Task-A | -                   | Task-B    | 2 дня            |
| Task-B | Task-A              | Task-C    | 1 день           |
| Task-C | Task-B              | -         | 3 дня            |
| Task-D | -                   | -         | 1 день           |

#### Извлечение независимых flows

#### Алгоритм группировки:
1. **Критический поток**: самая длинная цепочка зависимостей
2. **Независимые потоки**: задачи без входных зависимостей 
3. **Параллельные потоки**: задачи с разрешенными зависимостями
4. **Буферные потоки**: гибкие задачи для балансировки нагрузки

#### Критерии оптимизации:
- Минимизация времени выполнения критического пути
- Равномерная загрузка потоков (load balancing)
- Минимизация простоев при ожидании синхронизации

#### Структуру мета-плана:

```
ПЛАН-PARALLEL-[ИМЯ].md
├── Parallel-Flow-1/
│   ├── Flow-1-Critical-Path.md
│   ├── [ссылки на критические задачи]
├── Parallel-Flow-2/
│   ├── Flow-2-Independent.md  
│   ├── [независимые задачи]
├── Parallel-Flow-N/
│   ├── Flow-N-Support.md
│   ├── [поддерживающие задачи]
└── Sync-Points/
    ├── Milestone-1-Sync.md
    ├── Integration-Gates.md
    └── Final-Convergence.md
```

## 📋 ШАБЛОН МЕТА-ПЛАНА

```markdown
# Параллельное выполнение: [Название плана]

**Исходный план**: [ссылка на основной план]
**Количество потоков**: N
**Общее время оптимизированное**: X дней (было Y дней)
**Коэффициент ускорения**: Y/X

## Анализ зависимостей

### Критический путь (самый длинный)
Поток 1: Task-A → Task-B → Task-C (6 дней)

### Независимые задачи
- Task-D (1 день)
- Task-E (2 дня)  
- Task-F (1 день)

## Распределение по потокам

### 🔥 Flow 1: Критический путь
- [ ] [Task-A](./Parallel-Flow-1/Task-A.md) → 2 дня
- [ ] [Task-B](./Parallel-Flow-1/Task-B.md) → 1 день  
- [ ] [Task-C](./Parallel-Flow-1/Task-C.md) → 3 дня
**Общее время flow**: 6 дней

### ⚡ Flow 2: Независимые задачи  
- [ ] [Task-D](./Parallel-Flow-2/Task-D.md) → 1 день
- [ ] [Task-E](./Parallel-Flow-2/Task-E.md) → 2 дня
**Общее время flow**: 3 дня (параллельно с flow 1)

### 🛠 Flow N: Поддерживающие задачи
- [ ] [Support tasks...]

## Синхронизационные точки

### Milestone 1: День 2
- ✅ Task-A завершена → разблокирует Task-B
- ✅ Task-E завершена → готова к интеграции

### Milestone 2: День 3  
- ✅ Task-B завершена → разблокирует Task-C
- ✅ Task-D завершена → готова к интеграции

### Final Integration: День 6
- ✅ Все потоки завершены
- ✅ Интеграция результатов
- ✅ Финальная проверка

## Метрики оптимизации
- **Исходное время**: Y дней (последовательно)
- **Оптимизированное время**: X дней (параллельно)
- **Ускорение**: Y/X коэффициент
- **Загрузка flows**: [процент загрузки каждого flow]
```

## 🎯 ТРИГГЕРЫ АКТИВАЦИИ И ЗАПУСК АГЕНТА

### При обнаружении ключевых слов:
- "параллельное выполнение"
- "распараллелить план"  
- "независимые задачи"
- "потоки работы" 
- "concurrent execution"
- "оптимизация времени"
- "ускорить выполнение"

### НЕМЕДЛЕННО запустить Task tool:
```
subagent_type: "parallel-plan-optimizer"
description: "Analyze dependencies and create parallel execution plan"
prompt: "
Задача: Создать оптимизированную параллельную структуру выполнения для плана [НАЗВАНИЕ_ПЛАНА].

ЭТАПЫ РАБОТЫ:

1. АНАЛИЗ ПЛАНА:
   - Прочитай основной файл плана и все дочерние файлы
   - Извлеки все задачи с их временными оценками
   - Найди секции 'Входные зависимости' в каждой задаче
   - Составь полный список задач с зависимостями

2. ПОСТРОЕНИЕ ГРАФА ЗАВИСИМОСТЕЙ:
   - Создай матрицу блокировок (кто кого блокирует)
   - Найди критический путь (самая длинная цепочка)
   - Определи независимые задачи (без входных зависимостей)
   - Выяви задачи, которые можно выполнять параллельно

3. ГРУППИРОВКА В FLOWS:
   - Flow 1: критический путь (самые важные задачи)
   - Flow 2-N: независимые и параллельные задачи
   - Балансировка времени между flows (разница не >30%)
   - Определи синхронизационные точки (milestones)

4. СОЗДАНИЕ СТРУКТУРЫ:
   - Создай мета-план: ПЛАН-PARALLEL-[НАЗВАНИЕ].md
   - Создай каталоги: Parallel-Flow-1/, Parallel-Flow-2/, ..., Sync-Points/
   - Перемести/перелинкуй задачи по соответствующим flows
   - Обнови все обратные ссылки на родительские файлы
   - Создай файлы синхронизации с milestone checkpoints

5. РАСЧЕТ МЕТРИК:
   - Исходное время (последовательное выполнение)
   - Оптимизированное время (параллельное выполнение) 
   - Коэффициент ускорения = исходное/оптимизированное
   - Загрузка каждого flow в %
   - Время простоев на синхронизацию

РЕЗУЛЬТАТ: Готовый к использованию мета-план с полной параллельной структурой, всеми файлами и корректными ссылками.

ВАЖНО: Создай ВСЕ файлы и структуру, не только планы, но и реальные рабочие файлы.
"
```

## ⚙️ ДЕЛЕГИРОВАНИЕ АГЕНТУ

### ВАЖНО: НЕ делать вручную, использовать только Task tool!

Весь анализ и реализацию выполняет специализированный агент `parallel-plan-optimizer`.

### Агент автономно выполняет:
```
1. Анализ входного плана - чтение всех файлов
2. Извлечение задач и зависимостей  
3. Построение графа зависимостей (DAG)
4. Поиск критического пути
5. Создание матрицы блокировок
6. Группировка в оптимальные flows
7. Генерация мета-плана и структуры
8. Создание синхронизационных точек
9. Расчет метрик ускорения
```

### Результат от агента:
- Полная параллельная структура плана
- Все файлы и каталоги созданы
- Ссылки обновлены
- Готовый к исполнению мета-план

## 🔄 WORKFLOW ВЫПОЛНЕНИЯ

1. **Запуск параллельных flows** одновременно
2. **Мониторинг progress** каждого flow  
3. **Синхронизация на milestone points**
4. **Разрешение межflow зависимостей**
5. **Финальная интеграция результатов**

## 📊 ОПТИМИЗАЦИОННЫЕ СТРАТЕГИИ

### Load Balancing:
- Равномерное распределение временных оценок
- Перебалансировка при дисбалансе >30%
- Динамическое перераспределение задач

### Critical Path Optimization:
- Приоритет ресурсов критическому пути
- Минимизация задержек на критических задачах
- Буферизация для некритических flows

### Bottleneck Prevention:
- Мониторинг узких мест выполнения
- Превентивное перераспределение ресурсов
- Early warning для блокирующих задач

## 🎯 КРИТЕРИИ УСПЕХА

- ✅ Время выполнения сокращено минимум на 30%
- ✅ Все flows имеют загрузку 70-100%
- ✅ Простои между синхронизациями <10%
- ✅ Критический путь оптимизирован
- ✅ Зависимости корректно разрешены

## 🚨 ОСОБЫЕ СЛУЧАИ

### Сложные зависимости:
- Циклические зависимости → ошибка, требуют ручного разрешения
- Условные зависимости → создать альтернативные flows
- Ресурсные конфликты → очередность выполнения

### Балансировка:
- Перегруженные flows → перераспределение задач  
- Недогруженные flows → добавление буферных задач
- Критический путь >80% времени → пересмотр архитектуры

## 📈 МЕТРИКИ И АНАЛИТИКА

```
Коэффициент ускорения = Время_последовательное / Время_параллельное
Эффективность = (Общее время задач) / (Время критического пути * Количество flows)  
Загрузка flow = Время_задач_flow / Время_критического_пути
Простои = Время_ожидания / Общее_время_выполнения
```

## 🔧 ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

### Граф зависимостей (DAG):
```
class TaskDependencyGraph {
    nodes: Task[]
    edges: Dependency[]
    
    findCriticalPath(): Task[]
    findIndependentComponents(): Task[][]
    optimizeFlows(maxFlows: number): TaskFlow[]
}
```

### Генерация мета-плана:
```
1. Создать координирующий файл
2. Создать каталоги flows (Parallel-Flow-N/)
3. Переместить задачи в соответствующие flows
4. Обновить ссылки (относительные пути)
5. Создать синхронизационные файлы
```

Данное правило обеспечивает автоматическое создание оптимального параллельного плана выполнения с максимальным ускорением при сохранении всех зависимостей.