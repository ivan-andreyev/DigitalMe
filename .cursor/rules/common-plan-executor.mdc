---
description: Применять при исполнении любого файла плана работ. Если в пользовательском запросе встречаются слова или фразы: "исполнение плана", "выполнение плана", "реализация плана", "execute plan", "follow plan", "iterate plan", "декомпозиция плана", "checklist", "progress по плану", "поехали по плану", "поехали выполнять" и т.п. — всегда старайся трактовать это как запрос на исполнение плана работ и применяй данное правило. Если не уверен — уточни у пользователя, требуется ли исполнение по этому правилу.
alwaysApply: false
---
# Common Plan Executor

## Общие тезисы

- Строго следуй изложенным тут указаниям, если не получил иных явных указаний, противоречащих данному правилу

## Порядок выполнения плана работ

1. **Ознакомление**: Изучаем план целиком, включая перекрёстные ссылки и иерархию файлов
2. **Оценка прогресса**: Определяем ближайший по порядку/приоритету пункт к выполнению
3. **Декомпозиция работ**: Если задача слишком крупная (>3 дней) или требует разных типов мышления, разбиваем её на подзадачи по принципу объёма кода и контекста
4. **Исполнение**: Выполняем выбранную задачу до 90+% уверенности в завершении
5. **Отметка выполнения**: Проставляем чекбокс о выполнении **ТОЛЬКО ПОСЛЕ ВАЛИДАЦИИ** (см. раздел "ЖЕЛЕЗОБЕТОННОЕ ПРАВИЛО СИНХРОННОСТИ")
6. **ОБЯЗАТЕЛЬНАЯ ревизия плана**: После КАЖДОГО выполненного пункта/подпункта/раздела применяем `@common-plan-reviewer.mdc` для проверки качества, а при необходимости доработки/переработки/декомпозиции — `@common-plan-generator.mdc`
7. **Обновление архитектурной документации**: Если выполненная задача касается архитектуры, интерфейсов или компонентов — применить `@architecture-documenter.mdc` для синхронизации фактической архитектуры
8. **Переход к следующей задаче**: Возвращаемся к п.1

## Принципы работы с декомпозированными планами

### Работа с иерархией файлов
- **Всегда** следуй перекрёстным ссылкам между родительскими и дочерними файлами
- При выполнении дочерней задачи отмечай прогресс как в дочернем, так и в родительском файле
- Если дочерний файл требует дальнейшей декомпозиции, создавай новые подфайлы

### 🔒 ЖЕЛЕЗОБЕТОННОЕ ПРАВИЛО СИНХРОННОСТИ ПЛАНОВ
**КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО отмечать этап/фазу как завершенную без полной валидации:**

#### Обязательная процедура перед отметкой завершения этапа:
1. **СНАЧАЛА проверить ВСЕ дочерние файлы**:
   - Все ли ссылки на дочерние файлы имеют статус "ЗАВЕРШЕН"
   - Все ли критерии готовности в дочерних файлах отмечены как выполненные
   - Нет ли в дочерних файлах незакрытых задач со статусом `[ ]`

2. **ЗАТЕМ проверить координационный файл этапа**:
   - Все ли задачи этапа отмечены как `[x]`
   - Все ли критерии готовности этапа выполнены
   - Все ли выходные артефакты созданы

3. **ТОЛЬКО ПОСЛЕ ЭТОГО** отмечать в родительском плане этап как завершенный

#### ПРИНЦИП "СНИЗУ ВВЕРХ":
- **НЕЛЬЗЯ** завершать родительский файл, пока НЕ ЗАВЕРШЕНЫ **ВСЕ** дочерние файлы
- **НЕЛЬЗЯ** завершать этап, пока НЕ ВЫПОЛНЕНЫ **ВСЕ** критерии готовности
- **НЕЛЬЗЯ** полагаться на память - **ВСЕГДА ПРОВЕРЯЙ ФАЙЛЫ**

#### В случае несинхронности:
- **НЕМЕДЛЕННО остановить** работу
- **ОТКАТИТЬ** преждевременную отметку завершения
- **ЗАВЕРШИТЬ** все незакрытые задачи
- **ТОЛЬКО ТОГДА** корректно отметить завершение

#### Структура планов
**Правила структуры:** см. детальные правила в `@catalogization-rules.mdc` и `@common-plan-generator.mdc`

### 🔍 КРИТЕРИИ ДЕКОМПОЗИЦИИ И РЕВИЗИИ ПЛАНА

#### Когда требуется декомпозиция/ревизия:
- **Объём работ**: >3 дней работы
- **Контекстная сложность**: разные типы кода (Unity ≠ API ≠ UI)
- **Файловые лимиты**: >400 строк в файле плана
- **Логическая завершённость**: задачи не являются независимыми
- **Техническая актуальность**: изменились требования/зависимости

### ЗОЛОТОЕ ПРАВИЛО: НИКАКИХ УДАЛЕНИЙ ПОСЛЕ НАЧАЛА ИСПОЛНЕНИЯ
- **С момента начала исполнения плана НИЧЕГО НЕ УДАЛЯЕТСЯ!**
- Только пометки выполнения ✅/❌
- Только комментарии и ссылки  
- Только дополнения и уточнения
- **Полная историчность и прозрачность** эволюции планирования

### Принцип "СНАЧАЛА создаём, ЗАТЕМ обновляем"
- При декомпозиции **ВСЕГДА** сначала создавай дочерние файлы с полным содержимым
- Только после этого обновляй родительский файл, заменяя детали на ссылки
- **НИКОГДА** не удаляй контент из исходного файла - только помечай статусом

## Методология обязательной ревизии плана

### Когда проводить ревизию
- **ВСЕГДА** после завершения каждого пункта/подпункта/раздела
- **НЕ ТОЛЬКО** при обнаружении несоответствий
- Ревизия - это часть нормального рабочего процесса, а не исключительная мера

### Алгоритм ревизии

1. **Анализ выполненной работы**:
   - Сколько времени фактически заняла задача vs оценка
   - Какие неожиданные сложности возникли
   - Какие новые требования/зависимости обнаружены

2. **Оценка оставшихся задач**:
   - Применение `@common-plan-reviewer.mdc` для структурной проверки
   - Применение критериев декомпозиции из `@common-plan-generator.mdc`
   - Проверка принципа "один файл = один тип мышления"
   - Актуализация оценок времени

3. **Структурная ревизия**:
   - Проверка размеров файлов плана (лимит 400 строк)
   - Валидация перекрёстных ссылок
   - Обновление иерархии файлов при необходимости

4. **Принятие решения**:
   - **Продолжить без изменений** - если план актуален
   - **Обновить план** - изменить приоритеты/оценки
   - **Декомпозировать** - разбить крупные задачи
   - **Реструктурировать** - создать новые дочерние файлы

### Документирование ревизии
- Краткие выводы ревизии добавлять в комментариях к задачам
- При значительных изменениях - создавать запись в истории изменений плана
- Сохранять связь между причинами изменений и фактическими результатами работы

## Артефакты выполнения заданий

- Если ты выполняешь пункт на исследование, то обязательно отражай результаты исследования рядом с этим пунктом или (если эти результаты значительны по объёму, скажем, более 100 строк) выноси в отдельный файл, оставив ссылку возле соответствующего пункта, и перекрёстную ссылку на родительский файл

- Если ты выполняешь пункт на внесение конкретных изменений, обязательно обращайся к исходному плану и ищи подходящий пункт на исследование, результаты которого необходимо учитывать при внесении изменений (и даже при планировании внесения изменений)

## КРИТЕРИИ УСПЕХА

В каждом плане скорее всего присутствует раздел с критериями успеха, опирайся на них при оценке завершённости плана

## Отслеживание прогресса

### 🔍 ПРОЦЕДУРА ВАЛИДАЦИИ ПЕРЕД ОТМЕТКОЙ ЗАВЕРШЕНИЯ

#### Для обычных задач:
```bash
- [X] Задача выполнена
- [X] Результат соответствует критериям  
- [X] Артефакты созданы
```

#### Для этапов/фаз с дочерними файлами:
```bash
# Шаг 1: Проверка дочерних файлов
- [ ] Все дочерние файлы: задачи [x], критерии готовности [x]

# Шаг 2: Проверка координационного файла  
- [ ] Все ссылки на дочерние файлы имеют пометку "ЗАВЕРШЕН"
- [ ] Все критерии готовности этапа отмечены [x]
- [ ] Все выходные артефакты созданы

# Шаг 3: ТОЛЬКО ТОГДА отметить в главном плане
- [X] Этап ✅ ЗАВЕРШЕНА (дата: ДД.ММ.ГГГГ)
```

#### 🚨 При обнаружении ошибок:
1. **ОТКАТИТЬ** преждевременную отметку завершения
2. **ЗАВЕРШИТЬ** все незакрытые задачи  
3. **ТОЛЬКО ТОГДА** корректно отметить завершение

### Базовое отслеживание прогресса:
```
- [X] Задача

- [ ] [/Файл]
```

## Примеры кода

Для каждой практической секции предоставляй примеры кода в правильных блоках:
```csharp
// Код на C#
```

## Учет особых случаев

Явно отмечай особые случаи и потенциальные проблемы:
- Если пользователь явно об этом попросил, то выполняй план до полного завершения без подтверждения переходов между пунктами/фазами
- Обработка нестандартных форматов данных
- Обработка ошибок от внешних систем
- Пагинация и обработка больших объемов данных
- Обработка разных часовых поясов


## 🚨 ВАЖНОЕ НАПОМИНАНИЕ

**ЗОЛОТОЕ ПРАВИЛО СИНХРОННОСТИ:** Никогда не полагайся на память. Всегда проверяй файлы перед отметкой завершения. Следуй процедуре валидации выше.