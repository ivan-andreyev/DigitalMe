---
description: Применять при создании любого файла плана работ. Если в пользовательском запросе встречаются слова или фразы: "план", "планирование", "начнём с планирования", "запланируем", "дорожная карта", "roadmap", "work plan", "разработка плана", "структура работ", "итерация плана", "feature plan", "integration plan", "test plan" и т.п. — всегда пессимистично трактуй это как запрос на генерацию плана работ и если не уверен, что правило нужно применять — уточни у пользователя, требуется ли планирование по этому правилу.
alwaysApply: false
---
# Common Plan Generator

## Общие тезисы

- Строго следуй изложенным тут указаниям, если не получил иных явных указаний, противоречащих данному правилу

## Порядок разработки плана работ

- **Согласование и уточнения** — уточняющие вопросы, предложения от Агента
- **Базовая генерация** — первый набросок с общими блоками/разделами/фазами выполнения работ
- **Итерирование и декомпозиция** — улучшения и детализация
- **Проверка качества** — **ОБЯЗАТЕЛЬНО** применить `@common-plan-reviewer.mdc` → исправить нарушения → повторить цикл до консенсуса
- **Документирование архитектуры** — **ОБЯЗАТЕЛЬНО** применить `@architecture-documenter.mdc` для создания плановой архитектурной документации

### Финальный чек-лист генерации (MUST)
- [ ] Координатор снаружи каталога; внутри — только дочерние файлы
- [ ] Все родительские/дочерние ссылки корректны (локальная проверка без http)
- [ ] Нет файлов в корне каталогов, кроме координаторов
- [ ] Именование без опечаток, предсказуемые пути
- [ ] Размер каждого файла плана ≤400 строк (предупреждение >250 допустимо)
- [ ] Прогон PlanStructureValidator — без структурных нарушений
- [ ] Плановая архитектурная документация создана в Docs/Architecture/Planned/
- [ ] Ссылки на архитектурную документацию добавлены в план

## Процесс генерации плана

- Задавай уточняющие вопросы — если нет 90% уверенности в том, что ты понял всё правильно относительно ожиданий пользователя

- Предлагай — если у тебя есть идеи получше касательно чего бы то ни было (технология, паттерн, подход в общем), то не стесняйся их предлагать пользователю на этапе Согласования и уточнения

- Итерируйся — всегда делай сначала базовый набросок, затем структурируй план (по рекомендациям ниже), затем делай минимум две итерации декомпозиции и детализации шагов плана

- **Исправляй нарушения** — если `@common-plan-reviewer.mdc` нашел проблемы, применить специфические исправления для каждого типа нарушения

## Структурирование и форматирование

### 🔥 КАТАЛОГИЗАЦИЯ ПЛАНОВ

**ВАЖНО**: Все правила каталогизации определены в `@catalogization-rules.mdc` - это единственный источник истины для всех аспектов структурирования планов.

Обязательно изучи и применяй `@catalogization-rules.mdc`, который содержит:
- ЗОЛОТЫЕ ПРАВИЛА каталогизации (идентичность имён, размещение координаторов)
- Нейминг-конвенции для файлов и каталогов
- Правила декомпозиции и кросс-ссылок
- Шаблоны координационных файлов
- Чек-листы валидации структуры

### Архитектурная диаграмма (ОБЯЗАТЕЛЬНО)

**КРИТИЧНО**: Рядом с основным файлом плана ОБЯЗАТЕЛЬНО должен лежать файл с архитектурой:
- **Имя файла**: `{PLAN_NAME}-Architecture.md` (например, `Galactic-Idlers-Plan-Architecture.md`)
- **Содержимое**: ПОЛНАЯ диаграмма архитектурных зависимостей в формате mermaid или текстовом виде
- **Многоуровневость**: Может быть несколько диаграмм для разных уровней абстракции
- **Интеграция**: Ссылка на архитектурную диаграмму должна быть в основном файле плана
- **Соответствие**: Все пункты плана должны учитывать эту схему и ориентироваться на неё

**Структура архитектурного файла:**
```markdown
# {PLAN_NAME} - Архитектурная диаграмма

## Высокоуровневая архитектура
```mermaid
graph TB
    A[Слой A] --> B[Слой B]
    B --> C[Слой C]
```

## Детализированные компоненты
```mermaid
flowchart LR
    Component1 --> Component2
    Component2 --> Component3
```

## Зависимости между разделами плана
- Раздел 01 -> Раздел 02: данные моделей используются в framework
- Раздел 02 -> Раздел 03: тесты проверяют framework компоненты
```

**Обновление основного файла плана:**
```markdown
# План работ

**Архитектурная схема**: [Plan-Architecture.md](./Plan-Architecture.md)

## Разделы плана...
```


## Отслеживание прогресса

Всегда добавляй чекбоксы к пунктам плана для отслеживания прогресса:
```
- [ ] Задача

- [ ] [/Файл]
```

## LLM-friendly принципы планирования

### КРИТИЧНО: План ≠ Реализация
- **ПЛАН содержит**: архитектуру, структуру классов, интерфейсы полностью, сигнатуры методов, принципы, TODO-комментарии
- **ПЛАН НЕ содержит**: полную реализацию методов, детальную бизнес-логику, готовый продакшн код
- **Лимит файла плана**: максимум 400 строк
- **Принцип**: один файл = один контекст для понимания
- **Немедленная декомпозиция**: если файл превышает лимит - сразу разбивать на дочерние

### Что МОЖНО и НУЖНО включать в план:
- **Полные интерфейсы** с сигнатурами всех методов, свойств, событий
- **Структуры классов** с публичными/protected членами и их типами
- **Перечисления (enum)** полностью с описанием значений
- **Атрибуты и декораторы** для документирования назначения
- **Архитектурные паттерны** в виде каркасов классов
- **Dependency Injection регистрации** без реализации логики
- **Exception классы** с базовой структурой
- **DTO/Model классы** с полной структурой свойств

### Что НЕЛЬЗЯ включать в план:
- **Реализацию методов** - только сигнатуры с TODO
- **Бизнес-логику** - только архитектурное разделение ответственности  
- **Конкретные алгоритмы** - только принципы и подходы
- **Готовый продакшн код** - только каркас для реализации
- **Детальную обработку ошибок** - только типы исключений и принципы

### Правильные примеры кода в планах

**✅ ОТЛИЧНО** - полный интерфейс в плане:
```csharp
/// <summary>
/// Универсальный интерфейс для игрового фреймворка
/// </summary>
public interface IGameFramework
{
    /// <summary>
    /// Выполнить команду игры
    /// </summary>
    Task<TResponse> ExecuteAsync<TCommand, TResponse>(TCommand command) 
        where TCommand : IGameCommand<TResponse>;
    
    /// <summary>
    /// Подписаться на игровые события
    /// </summary>
    void Subscribe<TEvent>(IGameEventHandler<TEvent> handler) 
        where TEvent : IGameEvent;
        
    /// <summary>
    /// Получить текущее состояние игры
    /// </summary>
    TState GetGameState<TState>() where TState : class, IGameState;
}
```

**✅ ОТЛИЧНО** - структура класса с TODO (TODO-маркеры это ХОРОШО!):
```csharp
/// <summary>
/// Параметр фильтрации для запросов
/// </summary>
public class FilterParam
{
    public string Field { get; set; }
    public object Value { get; set; }
    public FilterOperator Operator { get; set; }

    // TODO: Validation (IsValid property)
    // TODO: Factory methods (Equals, Contains, Range, etc.)
    // TODO: Equality/ToString overrides
}
```

**ВАЖНО: TODO-маркеры в планах - это ПРАВИЛЬНО!**
- TODO-комментарии обозначают будущий код, который нужно реализовать
- Они являются планировочными индикаторами, НЕ ошибками
- TODO помогает структурировать архитектуру без полной реализации
- Большое количество TODO в плане - нормально и ожидаемо

**✅ ХОРОШО** - каркас класса с архитектурой:
```csharp
public class GameFramework : IGameFramework
{
    private readonly IMediator _mediator;
    private readonly ILogger<GameFramework> _logger;
    
    public GameFramework(IMediator mediator, ILogger<GameFramework> logger)
    {
        _mediator = mediator;
        _logger = logger;
    }
    
    public async Task<TResponse> ExecuteAsync<TCommand, TResponse>(TCommand command) 
        where TCommand : IGameCommand<TResponse>
    {
        // TODO: Validation pipeline
        // TODO: Logging request
        // TODO: Execute via mediator
        // TODO: Error handling
        // TODO: Logging response
        throw new NotImplementedException();
    }
    
    // TODO: Implement Subscribe method
    // TODO: Implement GetGameState method
}
```

**❌ ПЛОХО** - полная реализация в плане:
```csharp
public class FilterParam
{
    public string Field { get; set; } = string.Empty;
    public object Value { get; set; } = null!;
    public FilterOperator Operator { get; set; } = FilterOperator.Equals;

    public bool IsValid => !string.IsNullOrWhiteSpace(Field) && Value != null;

    public override string ToString() => $"{Field}{Operator}{Value}";

    public override bool Equals(object? obj)
    {
        return obj is FilterParam other &&
               Field == other.Field &&
               Equals(Value, other.Value) &&
               Operator == other.Operator;
    }

    public override int GetHashCode() => HashCode.Combine(Field, Value, Operator);

    public static FilterParam Equals(string field, object value) => new() { Field = field, Value = value };
    // ... ещё 20+ строк реализации
}
```

**❌ ОЧЕНЬ ПЛОХО** - готовая бизнес-логика в плане:
```csharp
public async Task<GameStateResponse> ExecuteCommandAsync(AddResourceCommand command)
{
    try 
    {
        _logger.LogInformation("Executing AddResource command for {ResourceType}", command.ResourceType);
        
        var validationResult = await _validator.ValidateAsync(command);
        if (!validationResult.IsValid)
        {
            throw new ValidationException(validationResult.Errors);
        }
        
        var currentState = await _gameStateRepository.GetByIdAsync(command.GameStateId);
        if (currentState == null)
        {
            throw new GameStateNotFoundException(command.GameStateId);
        }
        
        var resource = currentState.Resources.FirstOrDefault(r => r.Type == command.ResourceType);
        if (resource == null)
        {
            resource = new Resource { Type = command.ResourceType, Amount = 0 };
            currentState.Resources.Add(resource);
        }
        
        resource.Amount += command.Amount;
        
        await _gameStateRepository.UpdateAsync(currentState);
        await _eventPublisher.PublishAsync(new ResourceAddedEvent(command.GameStateId, command.ResourceType, command.Amount));
        
        return new GameStateResponse { Success = true, GameState = currentState };
    }
    catch (Exception ex)
    {
        _logger.LogError(ex, "Failed to execute AddResource command");
        throw;
    }
}
```

### Дополнительные правила для кода в планах

**✅ МОЖНО в планах:**
- Полные определения интерфейсов со всеми методами и свойствами
- Публичные свойства и поля классов с типами
- Конструкторы с параметрами и DI регистрацией
- Атрибуты валидации (`[Required]`, `[StringLength]`, `[Range]`)
- Перечисления (enum) с полным списком значений
- Exception классы с конструкторами и свойствами
- Базовые классы с виртуальными/абстрактными методами (только сигнатуры)
- Event и Delegate определения
- Generic ограничения (`where T : class, IInterface`)

**❌ НЕЛЬЗЯ в планах:**
- Реализацию методов (только сигнатура + `throw new NotImplementedException()` или TODO)
- LINQ запросы, циклы, условия внутри методов
- Конкретные строковые константы и magic numbers
- Реализацию алгоритмов (сортировки, поиска, валидации)
- Detailed error handling logic
- Async/await реализацию (только сигнатуры async методов)
- Конфигурационные значения (только структуру конфигурации)

### Примеры архитектурных элементов в планах

**✅ ОТЛИЧНО** - полное определение enum:
```csharp
/// <summary>
/// Типы ресурсов в игре
/// </summary>
public enum ResourceType
{
    Credits = 1,
    Energy = 2,
    Materials = 3,
    Research = 4,
    Experience = 5
}
```

**✅ ХОРОШО** - базовый класс с архитектурой:
```csharp
/// <summary>
/// Базовый класс для всех игровых команд
/// </summary>
public abstract class BaseGameCommand<TResponse> : IGameCommand<TResponse>
{
    public Guid CommandId { get; } = Guid.NewGuid();
    public DateTime Timestamp { get; } = DateTime.UtcNow;
    
    // TODO: Implement validation logic
    public virtual bool IsValid() => throw new NotImplementedException();
    
    // TODO: Command execution template method
    public abstract Task<TResponse> ExecuteAsync();
}
```

**✅ ХОРОШО** - каркас с TODO комментариями:
```csharp
// Структура класса с TODO комментариями
public class ExampleClass
{
    // TODO: Core properties
    // TODO: Validation methods  
    // TODO: Business logic
}
```

## Учет особых случаев

Явно отмечай особые случаи и потенциальные проблемы:
- Обработка нестандартных форматов данных
- Обработка ошибок от внешних систем
- Пагинация и обработка больших объемов данных
- Обработка разных часовых поясов

## Пример структуры плана

```markdown
# План разработки [Компонент]

## Общая информация
- **Название компонента**: ComponentName
- **Источник данных**: API Source
- **Таблица назначения**: Target Table
- **Особенность данных**: Special Data Format
- **Дополнительные требования**: Additional Requirements

## 1. Модели данных

### 1.1 Модели API
- [ ] Модель для API-запроса:
```csharp
public class ApiRequest { }
```
- [ ] Модель для API-ответа
- [ ] Вспомогательные модели

### 1.2 Модели данных (сущности БД)
- [ ] Основная сущность:
```csharp
public class Entity { }
```

### 1.3 Вспомогательные модели
- [ ] Параметры компонента

## 2. Абстракции (интерфейсы)
- [ ] Основной интерфейс компонента
- [ ] Интерфейсы для сервисов
- [ ] Интерфейсы для трансформации данных

## 3. Тесты
- [ ] Тесты трансформации данных
- [ ] Тесты основного компонента
- [ ] Тесты внешних взаимодействий
- [ ] Тесты пограничных случаев

## 4. Реализация
- [ ] Утилитарные классы
- [ ] Базовые классы
- [ ] Конкретные сервисы
- [ ] Основной компонент

## 5. Порядок разработки
- [ ] Написать модели данных
- [ ] Определить интерфейсы
- [ ] Написать тесты
- [ ] Реализовать компоненты в порядке:
  - [ ] Утилиты и трансформации
  - [ ] Базовые классы
  - [ ] Конкретные сервисы
  - [ ] Основной компонент
- [ ] Провести тестирование
```

**ВАЖНО:** После создания плана **ОБЯЗАТЕЛЬНО** применить `@common-plan-reviewer.mdc` для проверки качества и исправления нарушений.
