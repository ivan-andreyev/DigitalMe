---
description: "Применять TDD Bug Fixing методологию для исправления багов через failing тесты. ОБЯЗАТЕЛЬНО для: критических багов в production, регрессий, интеграционных проблем, багов безопасности, проблем производительности. РЕКОМЕНДУЕТСЯ для: core-логики, API интеграций, обработки данных, многопоточности, бизнес-логики. НЕ применять для: опечаток, простых UI багов, одноразовых скриптов, prototype кода. Ключевые слова для активации: 'баг', 'регрессия', 'не работает', 'проблема', 'ошибка', 'критический', 'production', 'падает тест', 'воспроизвести баг'."
alwaysApply: false
---
# TDD Bug Fixing — Методология исправления багов через тесты

## Что это такое

**TDD Bug Fixing** — это подход к исправлению ошибок, основанный на принципах Test-Driven Development. Сначала пишется failing тест, воспроизводящий проблему, затем код исправляется до тех пор, пока тест не станет зеленым.

## Базовый принцип

```
🔴 КРАСНЫЙ → 🟢 ЗЕЛЕНЫЙ → 🔵 РЕФАКТОРИНГ

1. Пишем тест, воспроизводящий баг (должен падать)
2. Исправляем минимальный код для прохождения теста
3. Рефакторим и улучшаем решение
```

## Когда применять

### ✅ ОБЯЗАТЕЛЬНО применять для:
- **Критических багов** в production (блокируют бизнес-процессы)
- **Регрессий** (что-то работало, потом сломалось)
- **Интеграционных проблем** между компонентами
- **Багов безопасности** и утечек данных
- **Проблем производительности** с конкретными метриками
- **Багов, которые трудно воспроизвести** вручную

### ⚠️ РЕКОМЕНДУЕТСЯ применять для:
- Любых багов в core-логике приложения
- Проблем с API и внешними интеграциями
- Багов, связанных с обработкой данных
- Проблем с многопоточностью и concurrency
- Логических ошибок в бизнес-процессах

### 🤔 МОЖНО НЕ применять для:
- Очевидных опечаток и syntax errors
- Простых багов UI/UX (цвет кнопки, отступы)
- Одноразовых скриптов и утилит
- Prototype и proof-of-concept кода

## Процесс TDD Bug Fixing

### Этап 1: Анализ и воспроизведение 🔍
```markdown
1. **Понять проблему**
   - Что должно происходить?
   - Что происходит на самом деле?
   - При каких условиях проявляется?

2. **Локализовать баг**
   - В каком компоненте/методе/классе?
   - На каком уровне (unit/integration/e2e)?
   - Есть ли зависимости от внешних систем?

3. **Определить тип теста**
   - Unit test для логических ошибок
   - Integration test для проблем взаимодействия
   - E2E test для пользовательских сценариев
```

### Этап 2: Написание failing теста 🔴
```csharp
[TestMethod]
[Description("Воспроизводит баг #1234: MsDeployer не получает HTTP 200 OK")]
public void BugReproduction_MsStopCommand_ShouldReturnHttp200()
{
    // Arrange - настройка условий, при которых проявляется баг
    var testMicroservice = StartTestMicroservice();
    var stopCommand = CreateStopCommand();

    // Act - выполнение действия, которое вызывает баг
    var response = SendStopCommand(testMicroservice, stopCommand);

    // Assert - проверка ожидаемого поведения
    // 🔴 ЭТОТ ТЕСТ ДОЛЖЕН ПАДАТЬ!
    Assert.AreEqual(HttpStatusCode.OK, response.StatusCode,
        "BUG: MsDeployer должен получать HTTP 200 OK, но получает таймаут");
}
```

### Этап 3: Проверка, что тест падает ❌
```bash
# Тест должен стабильно падать
dotnet test --filter "BugReproduction_MsStopCommand" 
# Expected: FAILED

# Если тест проходит — баг не воспроизведен!
# Нужно уточнить условия воспроизведения
```

### Этап 4: Минимальное исправление 🟢
```csharp
// Исправляем код минимально, чтобы тест прошел
public void StopMicroservice(IEllyCommand command)
{
    try
    {
        // Минимальное логирование для диагностики
        _logger.LogAsync($"Stop command received", MsgLevels.Warning);
        
        // Graceful shutdown с задержкой
        Task.Run(async () =>
        {
            await Task.Delay(200); // Время для HTTP ответа
            await _msHost.WebApplication.StopAsync(_msHost.AppCancellationToken);
        });
    }
    catch (Exception ex)
    {
        _logger.LogAsync($"Error in StopMicroservice: {ex}", MsgLevels.Error);
        throw; // HTTP 500
    }
}
```

### Этап 5: Проверка, что тест проходит ✅
```bash
dotnet test --filter "BugReproduction_MsStopCommand"
# Expected: PASSED ✅
```

### Этап 6: Рефакторинг и улучшения 🔵
```csharp
// Добавляем дополнительные тесты для edge cases
[TestMethod]
public void StopCommand_MultipleSimultaneous_ShouldHandleGracefully() { ... }

[TestMethod]
public void StopCommand_WithActiveTask_ShouldWaitOrForceStop() { ... }

// Улучшаем код после прохождения базового теста
public void StopMicroservice(IEllyCommand command)
{
    // Более продвинутая реализация с обработкой edge cases
}
```

## Типы тестов для Bug Fixing

### 1. Bug Reproduction Test
```csharp
[TestMethod]
[BugRepro("#1234", "Описание бага")]
public void BugRepro_1234_SpecificIssueDescription()
{
    // Точное воспроизведение условий бага
}
```

### 2. Regression Prevention Test
```csharp
[TestMethod]
[RegressionTest("v1.2.3", "Проблема появилась после релиза")]
public void Regression_v123_FeatureBrokeAfterRelease()
{
    // Тест предотвращает возврат бага в будущем
}
```

### 3. Edge Case Test
```csharp
[TestMethod]
[EdgeCase("Concurrent access")]
public void EdgeCase_ConcurrentStopCommands_ShouldNotCrash()
{
    // Тестирует граничные случаи, связанные с багом
}
```

## Преимущества подхода

### 🛡️ Защита от регрессий
- Баг больше никогда не вернется незамеченным
- Автоматическая проверка при каждом коммите
- CI/CD пайплайн ловит проблемы до продакшена

### 📚 Документирование проблем
- Тест становится живой документацией бага
- Показывает ожидаемое поведение системы
- Помогает новым разработчикам понять логику

### 🎯 Фокус на решении
- Четкий критерий готовности: тест зеленый = баг исправлен
- Предотвращает over-engineering и лишние изменения
- Минимальное вмешательство в существующий код

### 🔍 Улучшение качества кода
- Повышает тестовое покрытие в проблемных местах
- Выявляет архитектурные проблемы
- Стимулирует написание более testable кода

## Шаблоны и соглашения

### Именование тестов
```csharp
// Формат: BugRepro_{БагНомер}_{КраткоеОписание}
BugRepro_1234_MsStopCommandTimeout()

// Формат: Regression_{Версия}_{ЧтоСломалось}
Regression_v123_LoginFailsAfterUpdate()

// Формат: EdgeCase_{Сценарий}_{ОжидаемоеПоведение}
EdgeCase_ConcurrentAccess_ShouldNotDeadlock()
```

### Структура теста
```csharp
[TestMethod]
[Description("BUG #1234: Краткое описание проблемы")]
[Priority(1)] // Критичность бага
public void BugRepro_1234_ShortDescription()
{
    // Arrange: Настройка условий бага
    // - Подготовить тестовые данные
    // - Настроить моки и зависимости
    // - Создать окружение для воспроизведения
    
    // Act: Выполнение проблемного действия
    // - Один четкий вызов метода/API
    // - Измерение метрик, если нужно
    
    // Assert: Проверка ожидаемого результата
    // - Четкие ассерты с описанием ожидания
    // - Проверка не только результата, но и состояния
    
    // 🔴 ВАЖНО: Тест ДОЛЖЕН падать до исправления!
}
```

### Метки и категории
```csharp
[TestCategory("BugFix")]
[TestCategory("Critical")]
[TestCategory("Integration")]
[Owner("TeamLead")]
[Priority(1)]
```

## Интеграция с процессом разработки

### Workflow для критических багов
1. **Создание задачи** в трекере (Jira/Azure DevOps)
2. **Создание ветки** `bugfix/TASK-1234-short-description`
3. **Написание failing теста** с номером задачи
4. **Коммит failing теста**: `feat: add failing test for #1234`
5. **Исправление кода** до зеленого теста
6. **Коммит исправления**: `fix: resolve #1234 - short description`
7. **Pull Request** с обязательным review
8. **Деплой** после прохождения всех тестов

### CI/CD интеграция
```yaml
# azure-pipelines.yml
- task: DotNetCoreCLI@2
  displayName: 'Run Bug Reproduction Tests'
  inputs:
    command: 'test'
    projects: '**/*Tests.csproj'
    arguments: '--filter "TestCategory=BugFix" --logger trx'
  condition: always()
```

### Мониторинг качества
```csharp
// Метрики для отслеживания
- Количество bug reproduction тестов
- Процент багов, покрытых тестами
- Время между обнаружением и исправлением
- Количество регрессий (повторных багов)
```

## Примеры применения

### Критический баг в продакшене
```markdown
**Проблема**: Сервис оплаты возвращает 500 ошибку для заказов > 10000 руб

**TDD Bug Fixing подход**:
1. Тест: `BugRepro_5678_PaymentFailsForLargeOrders()`
2. Воспроизводим: создаем заказ на 15000 руб, вызываем оплату
3. Ассерт: ожидаем HTTP 200, получаем 500
4. Исправляем: находим overflow в расчете комиссии
5. Тест зеленый: проблема решена
6. Добавляем edge cases: 9999, 10000, 10001, 100000 руб
```

### Интеграционная проблема
```markdown
**Проблема**: После обновления API третьей стороны перестал работать импорт

**TDD Bug Fixing подход**:
1. Тест: `Regression_v234_ThirdPartyApiIntegrationFails()`
2. Воспроизводим: мокаем новый формат ответа API
3. Ассерт: ожидаем успешный импорт, получаем исключение
4. Исправляем: обновляем парсинг ответа
5. Тест зеленый: интеграция работает
6. Добавляем backward compatibility тест
```

## Заключение

**TDD Bug Fixing** — это не просто методология, это **культура качества**. Каждый исправленный таким образом баг делает систему надежнее и защищает от регрессий в будущем.

### Мантра команды:
> **"Нет теста — нет гарантии исправления!"**
> 
> **"Каждый баг — это пропущенный тест!"**
> 
> **"Красный → Зеленый → Уверенность!"**# TDD Bug Fixing — Методология исправления багов через тесты

## Что это такое

**TDD Bug Fixing** — это подход к исправлению ошибок, основанный на принципах Test-Driven Development. Сначала пишется failing тест, воспроизводящий проблему, затем код исправляется до тех пор, пока тест не станет зеленым.

## Базовый принцип

```
🔴 КРАСНЫЙ → 🟢 ЗЕЛЕНЫЙ → 🔵 РЕФАКТОРИНГ

1. Пишем тест, воспроизводящий баг (должен падать)
2. Исправляем минимальный код для прохождения теста
3. Рефакторим и улучшаем решение
```

## Когда применять

### ✅ ОБЯЗАТЕЛЬНО применять для:
- **Критических багов** в production (блокируют бизнес-процессы)
- **Регрессий** (что-то работало, потом сломалось)
- **Интеграционных проблем** между компонентами
- **Багов безопасности** и утечек данных
- **Проблем производительности** с конкретными метриками
- **Багов, которые трудно воспроизвести** вручную

### ⚠️ РЕКОМЕНДУЕТСЯ применять для:
- Любых багов в core-логике приложения
- Проблем с API и внешними интеграциями
- Багов, связанных с обработкой данных
- Проблем с многопоточностью и concurrency
- Логических ошибок в бизнес-процессах

### 🤔 МОЖНО НЕ применять для:
- Очевидных опечаток и syntax errors
- Простых багов UI/UX (цвет кнопки, отступы)
- Одноразовых скриптов и утилит
- Prototype и proof-of-concept кода

## Процесс TDD Bug Fixing

### Этап 1: Анализ и воспроизведение 🔍
```markdown
1. **Понять проблему**
   - Что должно происходить?
   - Что происходит на самом деле?
   - При каких условиях проявляется?

2. **Локализовать баг**
   - В каком компоненте/методе/классе?
   - На каком уровне (unit/integration/e2e)?
   - Есть ли зависимости от внешних систем?

3. **Определить тип теста**
   - Unit test для логических ошибок
   - Integration test для проблем взаимодействия
   - E2E test для пользовательских сценариев
```

### Этап 2: Написание failing теста 🔴
```csharp
[TestMethod]
[Description("Воспроизводит баг #1234: MsDeployer не получает HTTP 200 OK")]
public void BugReproduction_MsStopCommand_ShouldReturnHttp200()
{
    // Arrange - настройка условий, при которых проявляется баг
    var testMicroservice = StartTestMicroservice();
    var stopCommand = CreateStopCommand();

    // Act - выполнение действия, которое вызывает баг
    var response = SendStopCommand(testMicroservice, stopCommand);

    // Assert - проверка ожидаемого поведения
    // 🔴 ЭТОТ ТЕСТ ДОЛЖЕН ПАДАТЬ!
    Assert.AreEqual(HttpStatusCode.OK, response.StatusCode,
        "BUG: MsDeployer должен получать HTTP 200 OK, но получает таймаут");
}
```

### Этап 3: Проверка, что тест падает ❌
```bash
# Тест должен стабильно падать
dotnet test --filter "BugReproduction_MsStopCommand" 
# Expected: FAILED

# Если тест проходит — баг не воспроизведен!
# Нужно уточнить условия воспроизведения
```

### Этап 4: Минимальное исправление 🟢
```csharp
// Исправляем код минимально, чтобы тест прошел
public void StopMicroservice(IEllyCommand command)
{
    try
    {
        // Минимальное логирование для диагностики
        _logger.LogAsync($"Stop command received", MsgLevels.Warning);
        
        // Graceful shutdown с задержкой
        Task.Run(async () =>
        {
            await Task.Delay(200); // Время для HTTP ответа
            await _msHost.WebApplication.StopAsync(_msHost.AppCancellationToken);
        });
    }
    catch (Exception ex)
    {
        _logger.LogAsync($"Error in StopMicroservice: {ex}", MsgLevels.Error);
        throw; // HTTP 500
    }
}
```

### Этап 5: Проверка, что тест проходит ✅
```bash
dotnet test --filter "BugReproduction_MsStopCommand"
# Expected: PASSED ✅
```

### Этап 6: Рефакторинг и улучшения 🔵
```csharp
// Добавляем дополнительные тесты для edge cases
[TestMethod]
public void StopCommand_MultipleSimultaneous_ShouldHandleGracefully() { ... }

[TestMethod]
public void StopCommand_WithActiveTask_ShouldWaitOrForceStop() { ... }

// Улучшаем код после прохождения базового теста
public void StopMicroservice(IEllyCommand command)
{
    // Более продвинутая реализация с обработкой edge cases
}
```

## Типы тестов для Bug Fixing

### 1. Bug Reproduction Test
```csharp
[TestMethod]
[BugRepro("#1234", "Описание бага")]
public void BugRepro_1234_SpecificIssueDescription()
{
    // Точное воспроизведение условий бага
}
```

### 2. Regression Prevention Test
```csharp
[TestMethod]
[RegressionTest("v1.2.3", "Проблема появилась после релиза")]
public void Regression_v123_FeatureBrokeAfterRelease()
{
    // Тест предотвращает возврат бага в будущем
}
```

### 3. Edge Case Test
```csharp
[TestMethod]
[EdgeCase("Concurrent access")]
public void EdgeCase_ConcurrentStopCommands_ShouldNotCrash()
{
    // Тестирует граничные случаи, связанные с багом
}
```

## Преимущества подхода

### 🛡️ Защита от регрессий
- Баг больше никогда не вернется незамеченным
- Автоматическая проверка при каждом коммите
- CI/CD пайплайн ловит проблемы до продакшена

### 📚 Документирование проблем
- Тест становится живой документацией бага
- Показывает ожидаемое поведение системы
- Помогает новым разработчикам понять логику

### 🎯 Фокус на решении
- Четкий критерий готовности: тест зеленый = баг исправлен
- Предотвращает over-engineering и лишние изменения
- Минимальное вмешательство в существующий код

### 🔍 Улучшение качества кода
- Повышает тестовое покрытие в проблемных местах
- Выявляет архитектурные проблемы
- Стимулирует написание более testable кода

## Шаблоны и соглашения

### Именование тестов
```csharp
// Формат: BugRepro_{БагНомер}_{КраткоеОписание}
BugRepro_1234_MsStopCommandTimeout()

// Формат: Regression_{Версия}_{ЧтоСломалось}
Regression_v123_LoginFailsAfterUpdate()

// Формат: EdgeCase_{Сценарий}_{ОжидаемоеПоведение}
EdgeCase_ConcurrentAccess_ShouldNotDeadlock()
```

### Структура теста
```csharp
[TestMethod]
[Description("BUG #1234: Краткое описание проблемы")]
[Priority(1)] // Критичность бага
public void BugRepro_1234_ShortDescription()
{
    // Arrange: Настройка условий бага
    // - Подготовить тестовые данные
    // - Настроить моки и зависимости
    // - Создать окружение для воспроизведения
    
    // Act: Выполнение проблемного действия
    // - Один четкий вызов метода/API
    // - Измерение метрик, если нужно
    
    // Assert: Проверка ожидаемого результата
    // - Четкие ассерты с описанием ожидания
    // - Проверка не только результата, но и состояния
    
    // 🔴 ВАЖНО: Тест ДОЛЖЕН падать до исправления!
}
```

### Метки и категории
```csharp
[TestCategory("BugFix")]
[TestCategory("Critical")]
[TestCategory("Integration")]
[Owner("TeamLead")]
[Priority(1)]
```

## Интеграция с процессом разработки

### Workflow для критических багов
1. **Создание задачи** в трекере (Jira/Azure DevOps)
2. **Создание ветки** `bugfix/TASK-1234-short-description`
3. **Написание failing теста** с номером задачи
4. **Коммит failing теста**: `feat: add failing test for #1234`
5. **Исправление кода** до зеленого теста
6. **Коммит исправления**: `fix: resolve #1234 - short description`
7. **Pull Request** с обязательным review
8. **Деплой** после прохождения всех тестов

### CI/CD интеграция
```yaml
# azure-pipelines.yml
- task: DotNetCoreCLI@2
  displayName: 'Run Bug Reproduction Tests'
  inputs:
    command: 'test'
    projects: '**/*Tests.csproj'
    arguments: '--filter "TestCategory=BugFix" --logger trx'
  condition: always()
```

### Мониторинг качества
```csharp
// Метрики для отслеживания
- Количество bug reproduction тестов
- Процент багов, покрытых тестами
- Время между обнаружением и исправлением
- Количество регрессий (повторных багов)
```

## Примеры применения

### Критический баг в продакшене
```markdown
**Проблема**: Сервис оплаты возвращает 500 ошибку для заказов > 10000 руб

**TDD Bug Fixing подход**:
1. Тест: `BugRepro_5678_PaymentFailsForLargeOrders()`
2. Воспроизводим: создаем заказ на 15000 руб, вызываем оплату
3. Ассерт: ожидаем HTTP 200, получаем 500
4. Исправляем: находим overflow в расчете комиссии
5. Тест зеленый: проблема решена
6. Добавляем edge cases: 9999, 10000, 10001, 100000 руб
```

### Интеграционная проблема
```markdown
**Проблема**: После обновления API третьей стороны перестал работать импорт

**TDD Bug Fixing подход**:
1. Тест: `Regression_v234_ThirdPartyApiIntegrationFails()`
2. Воспроизводим: мокаем новый формат ответа API
3. Ассерт: ожидаем успешный импорт, получаем исключение
4. Исправляем: обновляем парсинг ответа
5. Тест зеленый: интеграция работает
6. Добавляем backward compatibility тест
```

## Заключение

**TDD Bug Fixing** — это не просто методология, это **культура качества**. Каждый исправленный таким образом баг делает систему надежнее и защищает от регрессий в будущем.

### Мантра команды:
> **"Нет теста — нет гарантии исправления!"**
> 
> **"Каждый баг — это пропущенный тест!"**
> 
> **"Красный → Зеленый → Уверенность!"**